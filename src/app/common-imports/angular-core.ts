export {   QueryList, ViewChildren,Renderer2,OnDestroy, OnInit, ViewChild,Input, Output, EventEmitter,DoCheck,ChangeDetectionStrategy,HostListener,SimpleChanges,
     ComponentRef, ChangeDetectorRef, ComponentFactoryResolver,OnChanges,AfterContentChecked, AfterViewInit, Type,AfterViewChecked, ElementRef } from '@angular/core';
export { AbstractControl, FormBuilder, Validators,FormArray, FormGroup,NgForm } from '@angular/forms';
export { DatePipe, DecimalPipe } from '@angular/common';
